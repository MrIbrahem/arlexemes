name: Auto Update Toolforge

on:
    push:
        branches:
            - main
jobs:
    update:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v6

            - name: Test workspace
              run: |
                  echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE" # /home/runner/work/arlexemes/arlexemes # ls -al

            - name: Copy files to server
              uses: appleboy/scp-action@v1.0.0
              with:
                  host: ${{ secrets.HOST }}
                  username: ${{ secrets.USERNAME }}
                  key: ${{ secrets.KEY }}
                  port: 22
                  source: "./"
                  target: "~/arlexemes_deploy_tmp"
                  rm: true

            - name: Deploy on Toolforge
              uses: appleboy/ssh-action@v1.2.4
              with:
                  host: ${{ secrets.HOST }}
                  username: ${{ secrets.USERNAME }}
                  port: 22
                  request_pty: true
                  script_stop: true
                  key: ${{ secrets.KEY }}
                  script: |
                      become arlexemes bash -lc "
                        rm -rf ~/up_repo &&
                        cp -r /mnt/nfs/labstore-secondary-tools-home/${{ secrets.USERNAME }}/arlexemes_deploy_tmp ~/up_repo &&
                        cp -r ~/up_repo/shs/* ~/shs &&
                        sed -i 's/\r$//' ~/shs/*.sh &&
                        chmod +x ~/shs/*.sh &&
                        toolforge-jobs run update --image python3.13 --command ~/shs/update.sh --wait &&
                        rm -rf ~/up_repo &&
                        echo \"Updated on: $(date '+%Y-%m-%d %H:%M:%S')\" > ~/update_from_github &&
                        toolforge-jobs run pipup --image python3.13 --command ~/shs/pip.sh --wait &&
                        toolforge-webservice python3.13 stop &&
                        toolforge-webservice python3.13 start &&
                        echo ''
                      "
                      rm -rf arlexemes_deploy_tmp
